# Headers for Netlify ModTools deployment.
# CORS headers are required because assets may be served from Netlify deploy preview URLs
# (via cdnURL/DEPLOY_URL) while the page is served from modtools.org.

/_nuxt/*
  Access-Control-Allow-Origin: *
  Access-Control-Allow-Headers: Origin, X-Requested-With, Content-Type, Accept, Authorization

/netlify/*
  Access-Control-Allow-Origin: *
  Access-Control-Allow-Headers: Origin, X-Requested-With, Content-Type, Accept, Authorization
